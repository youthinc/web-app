export interface FormValues {
  days: string;
  times: string;
  dayTimes: string;
  contact: string[];
  feedback: string;
}

export interface ServerValues {
  days: string;
  times: string;
  dayTimes: string;
  contact: string[];
  feedback: string;
}
